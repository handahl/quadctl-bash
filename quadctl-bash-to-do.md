# quadctl to do

I have not really understood the fzf, or maybe alternatively the tab-autocomplete function, that is a different thing, right? This should work for quadctl as well. also, I was thinking for services like dozzle-agent it should suffice to say quadctl start dozzle, when there is only dozzle-agent.

I am also unsure about the standalone command `quadctl start service` and being in the interactive shell `start service` (or other commands): are there reasons for why there could or maybe should be differences? 

Can we do this: I would like for quadctl to default to the shell in a way that when we are not in the shell, `quadctl any-cmd` will work as intended and then also drop us into the interactive shell and as per usual show the short version of the matrix? 

I also found some inconsistencies with how I had intended it to work, for example (since I have aliased quadctl to q) q s was my short form to get into the shell before, that would be redundant now. but q s a was intended to be the shell and all services matrix, but it does not currently work that way. 

and some other points. Please before we go for any implementation, let's discuss my ideas. Considering the restraints (which of course can also be changed, but need to be discussed first, too) and my goals for the application, and maybe the limits of bash or where other tools or other ways might be superior. 

---

### td1

``` log
quadctl> start kanidm-admin
[ERR]  Unit 'kanidm-admin.service' does not exist or is not loaded.
[INFO] Try: systemctl --user list-unit-files 'kanidm-admin.service*'
```

- this should not exit the interactive shell

- this should not recommend a .service name without adding the Prefix

---

### td2

About the Prefix:
``` bash
# from env.bash 21-33
# 3. PREFIX GOVERNANCE
# ------------------------------------------------------------------------------
# The default is "quadctl-".
# If the user has not explicitly set their preference, we warn them (once)
# but proceed with the safe default.

if [[ -z "${QUADCTL_PREFIX:-}" ]]; then
    export Q_ARCH_PREFIX="hanlab-"
    export Q_ENV_WARNING="true" 
else
    export Q_ARCH_PREFIX="${QUADCTL_PREFIX}"
    export Q_ENV_WARNING="false"
fi
```

I'm not sure why we need 2 variables for the prefix.

on that note: we might be able to create the files in our %h/src/containers/intent/ folder without the prefix, quadctl will then add the chosen prefix to the *.containerfiles and within them to ContainerName=
please feedback that idea against best practices

---

### td3

I would like to have some form of active feedback for when a service that started without an immediate failure but then keeps restarting. what I called the loopy! - caught in a restart loop. Currently we only know about it if we print the matrix again or look at the logs.

---

### td4

since I am not using the podman socket anymore with traefik but a dynamic-config.yml I do not have the labels for ROUTING anymore. I would propose to add my own org.hanlab.route= or which alternatives would you propose? 

---

### td5

.volume and .image 

as in:

```
# ~/src/containers/intent/hanlab-paperless-consume.volume
[Volume]
VolumeName=hanlab-paperless-consume
Device=/var/home/handahl/.local/share/hanlab/paperless/consume
Driver=local
Options=bind
Type=none
```

results in 

```
quadctl> cat paperless-consume-volume
:: [Runtime View] Querying systemd for 'paperless-consume-volume'...
   (Resolved to hanlab-paperless-consume-volume)
# /run/user/1001/systemd/generator/hanlab-paperless-consume-volume.service
# Automatically generated by /usr/lib/systemd/user-generators/podman-user-generator
# 
[X-Volume]
VolumeName=hanlab-paperless-consume
Device=/var/home/handahl/.local/share/hanlab/paperless/consume
Driver=local
Options=bind
Type=none

[Unit]
Wants=podman-user-wait-network-online.service
After=podman-user-wait-network-online.service
SourcePath=/var/home/handahl/.config/containers/systemd/hanlab-paperless-consume.volume
RequiresMountsFor=%t/containers

[Service]
ExecStart=/usr/bin/podman volume create --ignore --driver local --opt device=/var/home/handahl/.local/share/hanlab/paperless/consume --opt type=none --opt o=bind hanlab-paperless-consume
SyslogIdentifier=%N
Type=oneshot
RemainAfterExit=yes

# /usr/lib/systemd/user/service.d/10-timeout-abort.conf
# This file is part of the systemd package.
# See https://fedoraproject.org/wiki/Changes/Shorter_Shutdown_Timer.
#
# To facilitate debugging when a service fails to stop cleanly,
# TimeoutStopFailureMode=abort is set to "crash" services that fail to stop in
# the time allotted. This will cause the service to be terminated with SIGABRT
# and a coredump to be generated.
#
# To undo this configuration change, create a mask file:
#   sudo mkdir -p /etc/systemd/user/service.d
#   sudo ln -sv /dev/null /etc/systemd/user/service.d/10-timeout-abort.conf

[Service]
TimeoutStopFailureMode=abort
```

---

### td6

I was thinking about a cleanup command, for the matrix view only. If I remove a service from the intent folder and then on sync it is deleted for the quadlet generator sometimes I have leftovers in the view. 

like:

kanidm-admin            syncy    failed     failed     1d       missing    -            -

because when I stopped it something didn't happen properly or so. Or services I remove because they keep failing. 

as well as the exited ones, networks, still an issue and volumes

